# Optional: sends a confirmation email after the signup has been stored in D1.
# BREVO_API_KEY is a secret — set with: wrangler secret put BREVO_API_KEY
BREVO_API_KEY=""
WAITLIST_FROM_EMAIL="Impulsive <hello@useimpulsive.com>"
WAITLIST_FROM_NAME="Impulsive"
WAITLIST_REPLY_TO_EMAIL="hello@useimpulsive.com"
WAITLIST_ADMIN_EMAIL=""

# Required in production if you want client IPs stored as hashes instead of omitted.
WAITLIST_IP_HASH_SECRET=""
